Utiles_escolares = ("Papel", "Lapiz", "Marcador", "Cuaderno")
opcion = []
Util = []



while True: 
    menu = ("""
Menu
        1. Listar los utiles
        2. Agregar nuevos utiles
        3. Listar todos los utiles
        3. Actualizar un util
        4. Eliminar util 
        5.Salir
""")

    print(menu)
    opcion = input("Que quieres hacer: ")

    if opcion == "1":
        print (f"Productos Disponibles:")
        for posicion, item in enumerate(Utiles_escolares):
            print(f"{posicion+1}. {item}")
        input ("Presiona ENTER para Continuar...")

    if opcion == "2":
        nombre = (input("Ingrese el util a agregar: "))
        cantidad = int(input(f"Ingrese la cantidad :"))
        item.append(nombre)
        item.append(cantidad)
        item.append(item)
        print(Utiles_escolares)
        item.clear()
        print("El util ha sido agregado exitosamente.")
        input ("Presiona ENTER para Continuar...")

    if opcion == "3":
        Utiles_escolares
        
